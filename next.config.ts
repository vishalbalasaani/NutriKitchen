import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["plenty-spill-dried.ngrok-free.dev", "cuddly-vans-itch.loca.lt"],
};

export default nextConfig;
